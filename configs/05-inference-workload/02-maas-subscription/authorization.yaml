apiVersion: maas.opendatahub.io/v1alpha1
kind: MaaSAuthPolicy
metadata:
  annotations:
    openshift.io/description: Auth policy created for subscription "demo"
  name: demo-policy
  namespace: models-as-a-service
spec:
  modelRefs:
    - name: llama-3-2-1b-instruct
      namespace: model-server
  subjects:
    groups:
      - name: 'system:authenticated'
